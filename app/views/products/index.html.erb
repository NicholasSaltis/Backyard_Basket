<p id="notice"><%= notice %></p>

<h1 class="d-block text-center">Your Produce</h1>
<div class="container">
<table class="table">
  <thead>
    <tr>
      <th>Name</th>
      <th>Description</th>
      <th>Organic</th>
      <th>Harvested date</th>
      <th>Expiry date</th>
      <th>Stock</th>
      <th>Price per unit</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @products.each do |product| %>
      <tr>
        <td><%= link_to product.name, product %></td>
        <td><%= product.description %></td>
        <td><%= product.organic %></td>
        <td><%= product.harvested_date %></td>
        <td><%= product.expiry_date %></td>
        <td><%= product.stock %></td>
        <td><%= product.price_per_unit %></td>
        
      </tr>
      <tr>
        <td><span><%= link_to 'Edit', edit_product_path(product) %></span> <span><%= link_to 'Destroy', product, method: :delete, data: { confirm: 'Are you sure?' } %></span></td>
        
        
        
      </tr>
      
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Product', new_product_path, class: "btn btn-light text-primary" %>
</div>