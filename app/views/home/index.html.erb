<div class="jumbotron">
<h1 class="text-center display-1 pb-5">Backyard Basket</h1>
    <div class="row justify-content-center">
        <div class="col-sm-5">
            <div class="card">
                <h2 class="card-title mx-auto pt-4 pb-2">Welcome</h2>
                <p class="card-text px-5 pb-1">The Backyard Basket is a place for people to find a better use for the excess veggies, fuits, eggs or other home grown produce that often comes when a crop's season comes around. By linking you into your local community of Home Growers, your pantry or fridge can be stocked with fresh, home grown produce that avoids the detrimental effects of commercial agriculture and transport and allows you to diversify the produce you can get beyond what you backyard may be able to handle growing.</p>
                <p class="card-text px-5 pb-5">Backyard Basket combines the convenience of a modern marketplace, allowing you to view what people are growing or have overflowing from their pantry and easily list your own home grown products, with the age old practice of sharing your backyard bounty with your neighbours and friends</p>
            </div>
        </div>
        <% if user_signed_in?%>
        <% else %>
            <div class="col-sm-5">
                <div class="card">
                    <h2 class="card-title mx-auto pt-4 pb-2">Sign up</h2>
                    <%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
                    <%= f.error_notification %>

                    <div class="form-inputs mx-4">
                        <%= f.input :email,
                                    required: true,
                                    autofocus: true,
                                    input_html: { autocomplete: "email" }%>
                        <%= f.input :password,
                                    required: true,
                                    hint: ("#{@minimum_password_length} characters minimum" if @minimum_password_length),
                                    input_html: { autocomplete: "new-password" } %>
                        <%= f.input :password_confirmation,
                                    required: true,
                                    input_html: { autocomplete: "new-password" } %>
                    </div>

                    <div class="form-actions mx-4 my-4 text-center">
                        <%= f.button :submit, "Sign up", class: "btn btn-primary"%>
                    </div>
                    <% end %>

                    <%= render "devise/shared/links" %>
                </div>
            </div>
        <% end %>
        
        
        
    </div>
</div>
