<div class="navbar navbar-expand-lg navbar-light bg-light">
    <%=link_to "Backyard Basket", root_path, class: "navbar-brand mx-3" %>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse justify-content-between" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item active">
                <%=link_to "Browse", browse_path, class: "nav-link"%>
            </li>
            <li class="nav-item active">
                <%=link_to "Your Pantry", products_path, class: "nav-link"%>
            </li>
        </ul>
        <ul class="navbar-nav">
            
            <li class="nav-item active">
            <%if user_signed_in? && current_user.profile%>
                <%=link_to "Profile", profile_path(id: profile_id), class: "nav-link"%>
            <%end%>
            </li>
            <%if user_signed_in? && current_user.profile%>
                <li class="nav-item active">
                    <%=link_to "Sign out", destroy_user_session_path, method: :delete, class: "nav-link btn btn-danger"%>
                </li>
            <%else%>
                <li class="nav-item active">
                    <%=link_to "Sign in", root_path, class: "nav-link btn btn-success"%>
                </li>
            <%end%>
        </ul>
    </div>
</div>